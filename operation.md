## Inflation attack

the vault need to handle the inflation attack. It can be done by updating the decimal offset, or us always made the
initial deposit to avoid the vault in an empty state.

> TODO: calculate how much is needed
